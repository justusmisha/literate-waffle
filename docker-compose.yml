version: "3.9"

services:
  tg-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: always
    volumes:
      - ./logs:/app/logs  # Optional: Map logs directory for persistence
